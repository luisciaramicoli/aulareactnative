import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    input: {
        height: 80,
        textAlign: 'center',
        width: '50%',
        color: 'lightgray',
        fontSize:20,
    },
})
export default styles;